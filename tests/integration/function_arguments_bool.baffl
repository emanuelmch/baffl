fun getNumber(x: bool): i32 {
    if (x) {
        return 2;
    }
    if (y == false) {
        return 4;
    }
}

fun main() {
    let x = getNumber(false);
    let y = getNumber(true);
    return x + y;
}
